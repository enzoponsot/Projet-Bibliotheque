// ui.h - GTK UI header (single UI file)
#ifndef UI_H
#define UI_H

#include "structure.h"

// Run the GTK UI application; this function blocks until the GUI exits.
void run_ui_app(Bibliotheque *bib);

#endif // UI_H
// (GTK-only UI header)
